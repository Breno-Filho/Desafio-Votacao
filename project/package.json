{
  "name": "cooperative-voting-api",
  "version": "1.0.0",
  "description": "API REST para sistema de votação cooperativa usando Spring Boot",
  "private": true,
  "scripts": {
    "start": "mvn spring-boot:run",
    "test": "mvn test",
    "build": "mvn clean package"
  }
}